---
title: Drupal
description: Add grounded-ui as a theme library, keep CKEditor from stripping its attributes, and stay safe under CSS aggregation.
---


## Test your Drupal forms first

Before changing anything, run the contracts against a page of your existing site. It shows what already works and what doesn't, whether or not you use Grounded UI's CSS:

```sh
npx grounded-conformance https://your-site.example/contact --binding your-theme.text-field.binding.yaml
```

See [Test your components](/getting-started/test-your-components) for writing the binding.

## Define a library

```yaml
# mytheme.libraries.yml
grounded:
  version: 0.1.0
  css:
    component:
      grounded-ui/reference/core/core.css: {}
      grounded-ui/reference/text-field/text-field.css: {}
      grounded-ui/reference/dialog/dialog.css: {}
```

Attach it globally:

```yaml
# mytheme.info.yml
libraries:
  - mytheme/grounded
```

## CKEditor 5

Load the same CSS in the editor so content previews correctly:

```yaml
# mytheme.info.yml
ckeditor5-stylesheets:
  - grounded-ui/reference/core/core.css
  - grounded-ui/reference/text-field/text-field.css
```

Restricted text formats strip unknown attributes. Allow the hooks the components use in the format's **Allowed HTML tags**, for example:

```html
<div data-component data-part data-variant> <label for data-part> <p id data-part>
<dialog id data-component data-variant aria-labelledby aria-describedby closedby role>
<button type commandfor command>
```

## CSS aggregation

Aggregation combines and may reorder the files. That's safe: every Grounded UI file declares the full layer order at the top, so the first file to load sets the same order. See [cascade layers](/styling/cascade-layers).

## Single Directory Components

A Grounded UI component maps directly onto an SDC: copy the markup into `component.twig`, put the stylesheet next to it, and read attributes from props. Generated SDC output is on the roadmap.
