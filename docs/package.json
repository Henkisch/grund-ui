{
  "name": "docs",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/budget.mjs >/dev/null && node ../scripts/consistency.mjs >/dev/null && node scripts/generate.mjs",
    "dev": "blume dev",
    "prebuild": "node ../scripts/budget.mjs >/dev/null && node ../scripts/consistency.mjs >/dev/null && node scripts/generate.mjs",
    "build": "blume build",
    "doctor": "blume doctor",
    "preview": "blume preview",
    "validate": "blume validate"
  },
  "dependencies": {
    "blume": "^2.0.3",
    "yaml": "^2.9.1"
  }
}
